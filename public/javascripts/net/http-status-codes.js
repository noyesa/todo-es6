/**
 * HTTP status code returned from the other end when the request has succeeded.
 * @const {number}
 */
export const ok = 200;

/**
 * HTTP status code returned from the other end when the request has succeeded
 * but the content of the document returned has not changed.
 * @const {number}
 */
export const notModified = 304;